# three-particles

Three.js skeleton project

## Install

```shell
$ npm install -g browserify budo uglify-es runjs
$ cd <install_dir>
$ npm install
```

## Usage

Start LiveUpdate loop via

```shell
$ run start
```

Build bundle.js via

```shell
$ run build
```
